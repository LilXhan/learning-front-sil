const title = document.getElementById('title')

// to use querySelector we must know that we have certain rules
// Example:
// to use id: #id
// to use class: .class
// to use tag: tag

// search body tag
const body = document.querySelector('body')

// search list sorted by id
const listSorted = document.querySelector('#ordered-list')

// search list unordered by class
const listUnordered = document.querySelector('.unordered-list')